import { hideFunction, showFunction } from "./events.js";

/** PARA CHAMAR PRECISA USAR (node --experimental-modules index.mjs) (VERSÃO NODE 12) */

/** NAS VERSÕES ATUAIS DO NODE (node index.mjs) */
hideFunction();
/** --------------------------------------------------------------- */
